@model IEnumerable<AE_Projet.Models.Filiere>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="AjtF">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nom_Filiere)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nom_Filiere)
            </td>
            <td>
                <a asp-action="MdfF" asp-route-id="@item.Id_Filiere">Edit</a> |
                <a asp-action="SuppF" asp-route-id="@item.Id_Filiere">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
